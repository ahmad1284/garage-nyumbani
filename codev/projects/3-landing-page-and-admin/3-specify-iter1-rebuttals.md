# Spec 3 — Specify Iteration 1 Rebuttals

## Gemini — SKIPPED
Quota exhausted. No feedback to address.

## Codex — SKIPPED
Not installed. No feedback to address.

## Claude — COMMENT (all addressed)

**Search bar UX ambiguity** → Fixed: spec now says filter pattern (hide non-matching cards), not scroll-to.

**Privacy trade-off undocumented** → Fixed: explicit note in spec that open phone lookup is accepted MVP trade-off; bookings contain no sensitive financial data.

**"Records" tab vs "Reminders" tab** → Fixed: clarified in spec that "Records" refers to existing Reminders tab; no new tab added.

**Africa's Talking "optionally" ambiguous** → Fixed: SMS button hidden when `AT_API_KEY` not set; behavior is now explicit.
